<?php

namespace Coff\Hellfire\Event;

class CyclicEvent extends Event
{
    const
        EVERY_3_SECOND       =   'every.3s.call',
        EVERY_10_SECOND    =   'every.10s.call',
        EVERY_15_SECOND    =   'every.15s.call',
        EVERY_30_SECOND    =   'every.30s.call',
        EVERY_MINUTE       =   'every.1m.call',
        EVERY_2_MINUTE     =   'every.2m.call',
        EVERY_10_MINUTE     =   'every.10m.call'
    ;

}
